<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payment Success - Khalti</title>
    <link
      href="https://cdn.jsdelivr.net/npm/lucide@latest/dist/umd/lucide.min.js"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      .fade-in {
        opacity: 0;
        transform: translateY(20px);
        animation: fadeInUp 1s ease-out forwards;
      }
      @keyframes fadeInUp {
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body
    class="bg-gradient-to-br from-emerald-50 via-white to-teal-50 flex items-center justify-center min-h-screen p-4"
  >
    <div class="w-full max-w-2xl fade-in">
      <!-- Success Header -->
      <div class="text-center mb-8">
        <div
          class="inline-flex items-center justify-center w-20 h-20 bg-emerald-100 rounded-full mb-4 animate-pulse"
        >
          <i data-lucide="check-circle" class="w-12 h-12 text-emerald-600"></i>
        </div>
        <h1 class="text-4xl font-bold text-gray-900 mb-2">
          Payment Successful!
        </h1>
        <p class="text-gray-600 text-lg">
          Your transaction has been processed successfully
        </p>
      </div>

      <!-- Main Card -->
      <div
        class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden"
      >
        <!-- Amount Section -->
        <div
          class="bg-gradient-to-r from-emerald-600 to-teal-600 text-white text-center p-6"
        >
          <p
            class="text-emerald-100 text-sm font-medium uppercase tracking-wide mb-2"
          >
            Amount Paid
          </p>
          <p class="text-4xl font-bold mb-2">NPR <%= paymentData.amount %></p>
          <div
            class="flex flex-col sm:flex-row items-center justify-center sm:space-x-4 text-emerald-100 text-sm space-y-1 sm:space-y-0"
          >
            <span>Fee: NPR <%= paymentData.dataFromVerificationReq.fee %></span>
            <span class="hidden sm:inline">•</span>
            <span class="flex items-center">
              <div class="w-2 h-2 bg-emerald-400 rounded-full mr-2"></div>
              <%= paymentData.status.charAt(0).toUpperCase() +
              paymentData.status.slice(1) %>
            </span>
          </div>
        </div>

        <!-- Transaction Details -->
        <div class="p-6">
          <h2 class="text-xl font-semibold text-gray-900 mb-6">
            Transaction Details
          </h2>

          <div class="space-y-4 text-sm">
            <div class="flex justify-between items-center border-b pb-3">
              <span class="text-gray-600 font-medium">Transaction ID</span>
              <span class="font-mono bg-gray-50 px-3 py-1 rounded-lg"
                ><%= paymentData.transactionId %></span
              >
            </div>
            <div class="flex justify-between items-center border-b pb-3">
              <span class="text-gray-600 font-medium">Payment ID (PIDX)</span>
              <span class="font-mono bg-gray-50 px-3 py-1 rounded-lg"
                ><%= paymentData.pidx %></span
              >
            </div>
            <div class="flex justify-between items-center border-b pb-3">
              <span class="text-gray-600 font-medium">Product</span>
              <span class="text-gray-900"
                ><%= paymentData.apiQueryFromUser.purchase_order_name %></span
              >
            </div>
            <div class="flex justify-between items-center border-b pb-3">
              <span class="text-gray-600 font-medium">Mobile Number</span>
              <span class="text-gray-900"
                ><%= paymentData.apiQueryFromUser.mobile %></span
              >
            </div>
            <div class="flex justify-between items-center border-b pb-3">
              <span class="text-gray-600 font-medium">Payment Method</span>
              <span class="text-gray-900"
                ><%= paymentData.paymentGateway === 'khalti' ? 'Khalti Digital
                Wallet' : paymentData.paymentGateway %></span
              >
            </div>
            <div class="flex justify-between items-center">
              <span class="text-gray-600 font-medium">Payment Date</span>
              <span class="text-gray-900">
                <%= new Date(paymentData.paymentDate).toLocaleString('en-US', {
                year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit',
                minute: '2-digit', hour12: true }) %>
              </span>
            </div>
          </div>

          <!-- Status Badge -->
          <div class="mt-6 flex justify-center">
            <div
              class="inline-flex items-center px-4 py-2 bg-emerald-50 border border-emerald-200 rounded-full"
            >
              <div
                class="w-2 h-2 bg-emerald-500 rounded-full mr-2 animate-pulse"
              ></div>
              <span class="text-emerald-700 font-medium text-sm"
                >Transaction Verified</span
              >
            </div>
          </div>

          <!-- Action Buttons -->
          <div class="mt-8 flex flex-col gap-4">
            <button
              class="w-full flex items-center justify-center px-6 py-3 bg-gray-100 hover:bg-gray-200 text-gray-700 font-medium rounded-xl text-base"
            >
              <i data-lucide="download" class="w-5 h-5 mr-2"></i>
              Download Receipt
            </button>
            <a
              href="http://localhost:5173"
              class="w-full flex items-center justify-center px-6 py-3 bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-700 hover:to-teal-700 text-white font-medium rounded-xl text-base transform hover:scale-105 transition-all"
            >
              <i data-lucide="home" class="w-5 h-5 mr-2"></i>
              Back to Home
            </a>
          </div>

          <!-- Footer Note -->
          <div class="mt-8 pt-6 border-t border-gray-100">
            <p class="text-center text-sm text-gray-500">
              Keep this confirmation for your records. For any queries, contact
              our support team.
            </p>
            <p class="text-center text-sm text-red-500">
              We will Call you when the Order is ready for delivery.
            </p>
          </div>
        </div>
      </div>

      <!-- Footer Note -->
      <div class="mt-6 text-center">
        <p class="text-gray-500 text-sm">
          Powered by <span class="font-semibold text-purple-600">Khalti</span> •
          Secure Payment Gateway
        </p>
      </div>
    </div>

    <script>
      lucide.createIcons();
    </script>
  </body>
</html>
